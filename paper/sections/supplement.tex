\section{Supplementary Material}
\label{sec:supplement}

This supplement provides the full proofs of Theorems~\ref{thm:hessian}
and~\ref{thm:twoscale}, additional experiments referenced in the main
text, and robustness checks.

\subsection{Full proof of Theorem~\ref{thm:hessian}}
\label{sec:proof_thm1}

We restate Theorem~\ref{thm:hessian} for reference:

\begin{quote}
\emph{Under Assumptions \ref{ass:linear}--\ref{ass:inputlip} and the
head hypothesis of Lemma~\ref{lem:phi_scaling}, the module curvature
disparity satisfies, in expectation over random Kaiming
(variance-scaled Gaussian) initialization,
$\mathcal{D}_{\mathrm{curv}}^{(M)} \geq c_1 \cdot M - c_2$, and for
architecture families with $\Cg = \Theta(M^2)$,
$\mathcal{D}_{\mathrm{curv}} \geq c_1' \sqrt{\Cg/\Cf} - c_2$, for
constants depending on the data distribution, the initialization
scheme, the bottleneck dimension $K$, the input spectral dimension
$S$, and the architecture class of $g_\phip$---but not on $M$ or
$\Cg$.}
\end{quote}

The proof rests on two lemmas: (i) the spatial block's top eigenvalue
scales linearly with width $M$ (Lemma~\ref{lem:phi_scaling}, proved
by an explicit witness direction in the head weights, with Karakida
et al.'s mean-field law as the sharp asymptotic benchmark); (ii) the
spectral block's top eigenvalue is capped by a $\Cg$-independent
operator-norm bound (Lemma~\ref{lem:schur}). Cauchy interlacing
(Lemma~\ref{lem:interlacing}) is retained as an auxiliary fact but is
not load-bearing for the disparity bound. We prove each lemma in
turn, then combine.

\subsubsection{Proof of Lemma~\ref{lem:phi_scaling}}

We show $\lambda_{\max}(J_\phip^\top J_\phip) = \Omega(M)$ in
expectation over random Kaiming (variance-scaled Gaussian)
initialization, under the head hypothesis of
Lemma~\ref{lem:phi_scaling}: the classifier head of $g_\phip$ is a
dense layer over a penultimate feature vector
$h \in \mathbb{R}^{M_h}$ with $M_h = \Theta(M)$ and $\Theta(1)$
per-entry second moment at initialization. The proof has three
steps: (1) context---Karakida et al.'s exact width-linear law for
the top Fisher eigenvalue in their model class, which we use as the
asymptotic benchmark but do \emph{not} invoke directly; (2) an exact
spectral lower bound on the softmax inner matrix at initialization;
(3) an elementary explicit witness direction in the head weights
that yields $\Omega(M)$ self-containedly. Throughout, Grams are
normalized as empirical means over the
$N := N_{\mathrm{data}} HW$ pixel positions; the unnormalized
(summed) Grams differ by the fixed factor $N$, which does not affect
any statement in $M$.

\paragraph{Step 1: The width-linear law of Karakida et al.\ (context).}
Karakida, Akaho, and Amari~\cite{karakida2019universal} analyze fully
connected networks of depth $L \geq 2$ with layer widths
$M_\ell = \alpha_\ell M$, linear outputs, and variance-scaled Gaussian
(Kaiming-type) initialization
$W^\ell_{ij} \sim \mathcal{N}(0, \sigma_w^2 / M_{\ell-1})$,
$b^\ell_i \sim \mathcal{N}(0, \sigma_b^2)$. For the empirical Fisher
information of the squared-loss model,
$F = \tfrac{1}{T} \sum_{t=1}^{T} \sum_{k} \nabla f_k(x_t) \nabla
f_k(x_t)^\top$ (the empirical-mean Gram of the output Jacobian),
their Theorem~4 gives, in the limit $M \gg 1$,
\[
    \lambda_{\max}(F)
    \;=\; \alpha \left( \tfrac{T-1}{T}\,\kappa_2
        + \tfrac{1}{T}\,\kappa_1 \right) M
    \;=\; \Theta(M),
\]
where $\kappa_1, \kappa_2 = \Theta(1)$ are macroscopic constants
computed from the layer-wise signal-propagation recursions and
$\alpha = \sum_\ell \alpha_\ell \alpha_{\ell - 1}$. Their Theorem~1
simultaneously shows the \emph{mean} eigenvalue is
$m_\lambda = C \kappa_1 / M = \mathcal{O}(1/M)$: as the network
widens, the FIM spectrum becomes extremely skewed---almost all
directions flatten while the top eigenvalue grows linearly in width.
We record two cautions. First, Theorem~4 assumes \emph{all} layer
widths scale proportionally with $M$; it does not cover networks
with width-independent input or output dimensions (such as a
classifier reading a fixed bottleneck $K$, where its coefficient
$\alpha$ degenerates), so it cannot be applied verbatim to our
spatial modules. Second, both statements are asymptotic mean-field
equalities with no finite-$M$ rate. Steps~2--3 therefore prove the
$\Omega(M)$ bound self-containedly; Karakida's law serves as the
sharp benchmark within its model class, validated numerically in
their Figure~1.

\paragraph{Step 2: The softmax inner matrix at initialization.}
Our loss is softmax cross-entropy, not squared loss, so the spatial
Gauss--Newton block carries the softmax inner matrix
\[
    G_{\phip\phip}
    \;=\; \frac{1}{N} \sum_{n=1}^{N}
        J^{(n)\top} H_\tau^{(n)} J^{(n)},
    \qquad
    H_\tau \;:=\; \mathrm{diag}(p) - p p^\top,
\]
where $J^{(n)} := \partial \hat{y}^{(n)} / \partial \phip$ is the
per-pixel logit Jacobian and $p = \mathrm{softmax}(\hat{y})$. For any
$v \in \mathbb{R}^{N_{cls}}$, the quadratic form is a variance:
$v^\top H_\tau v = \sum_i p_i v_i^2 - (\sum_i p_i v_i)^2 =
\mathrm{Var}_{i \sim p}(v_i) \geq p_{\min} \sum_i (v_i - \bar{v}_p)^2
\geq p_{\min} \norm{P_\perp v}^2$, where
$p_{\min} := \min_i p_i$, $\bar{v}_p := \sum_i p_i v_i$, the last
inequality uses that the unweighted mean minimizes the sum of squared
deviations, and $P_\perp := I - \tfrac{1}{N_{cls}}
\mathbf{1}\mathbf{1}^\top$ projects onto the mean-zero subspace
$\mathbf{1}^\perp$. Hence
\[
    H_\tau \;\succeq\; p_{\min} \, P_\perp .
\]
At Kaiming initialization the logits are $\Theta(1)$ (each
pre-activation has $\mathcal{O}(1)$ variance uniformly in $M$), so
$p_{\min} \geq c_p > 0$ with high probability over initialization and
data, for a constant $c_p$ of order $1/N_{cls}$ independent of $M$
and $\Cg$.

\paragraph{Step 3: An explicit witness direction.}
Write the classifier head of $g_\phip$ as $\hat{y} = W h + b$ with
penultimate features $h = h(x) \in \mathbb{R}^{M_h}$,
$M_h = \Theta(M)$ (for per-pixel classification, $h$ is the feature
vector at the classified pixel). Fix a unit contrast
$c \in \mathbb{R}^{N_{cls}}$ with $c \perp \mathbf{1}$;
Cauchy--Schwarz gives $P_\perp \succeq c c^\top$ (for any $u$,
$(c^\top u)^2 = (c^\top P_\perp u)^2 \leq \norm{P_\perp u}^2$).
Select a single pixel $n = 1$ with features $h_1 := h(x_1)$ and take
the unit parameter direction supported on the head weights,
\[
    v \;=\; c \otimes \frac{h_1}{\norm{h_1}}
    \;\in\; \mathbb{R}^{N_{cls} M_h},
    \qquad \norm{v} = 1 .
\]
Since $\partial \hat{y}_k / \partial W_{k'j} = \delta_{kk'} h_j$, we
have $c^\top J^{(n)} v = \norm{c}^2 \, (h_n^\top h_1)/\norm{h_1} =
(h_n^\top h_1)/\norm{h_1}$ for every pixel $n$. Every term of the
sum defining $G_{\phip\phip}$ is nonnegative ($H_\tau$ is PSD), so
dropping all but the $n=1$ term and applying Step~2 at pixel~1,
\[
    \lambda_{\max}(G_{\phip\phip})
    \;\geq\; v^\top G_{\phip\phip} v
    \;\geq\; \frac{p^{(1)}_{\min}}{N}
        \left( c^\top J^{(1)} v \right)^2
    \;=\; \frac{p^{(1)}_{\min}}{N}\, \norm{h_1}^2 ,
\]
where $p^{(1)}_{\min}$ is the smallest predicted probability at
pixel~1. Two initialization facts finish the proof.
(i)~By the head hypothesis of Lemma~\ref{lem:phi_scaling},
$\norm{h_1}^2 \geq q_h M_h$ with probability at least
$1 - \delta_h$, $\delta_h < 1/2$, over initialization.
(ii)~The single pixel's logits are $\Theta(1)$ at initialization
uniformly in $M$ (the head weights have variance $1/M_h$ against
$\norm{h_1}^2 = \mathcal{O}(M_h)$), so choosing the constant
$c_p > 0$ (of order $1/N_{cls}$) small enough,
$p^{(1)}_{\min} \geq c_p$ with probability at least $1 - \delta_p$
with $\delta_p \leq 1/4$. Since $\delta_h + \delta_p < 3/4$, the two
events intersect with probability at least $1/4$, and on that
intersection,
\[
    \lambda_{\max}(G_{\phip\phip})
    \;\geq\; \frac{c_p\, q_h}{N} \, M_h
    \;=\; \Omega(M).
\]
Since $\lambda_{\max} \geq 0$ always, taking expectations gives
$\E[\lambda_{\max}(G_{\phip\phip})] \geq
(1 - \delta_h - \delta_p)\,(c_p q_h / N)\, M_h \geq
\tfrac{1}{4}(c_p q_h / N) M_h = \Omega(M)$. The
$1/N$ factor is an artifact of the single-witness bound: the
discarded terms are nonnegative, and for activations with nonzero
mean at initialization (e.g.\ ReLU) the mean-feature witness
$u = \bar{h}/\norm{\bar{h}}$ removes it. In any case $N$ is fixed as
$M$ grows.

On the head hypothesis itself: for fully connected modules at
Kaiming Gaussian initialization it holds with any fixed
$\delta_h$---per-entry second moments are $\Theta(1)$ (the
signal-propagation recursions of Karakida et
al.~\cite{karakida2019universal}, Eq.~(9)), and the squared norm, a
sum of $M_h$ weakly dependent $\Theta(1)$ terms, concentrates around
its mean. For CNN and ViT heads we state the hypothesis as an
explicit assumption rather than a theorem, and check it empirically
in Section~\ref{sec:experiments}.

\paragraph{Residual-Jacobian form.}
When the block is written with the residual Jacobian
$J_{\phip} = H_\tau J^{(n)}$ (the convention of
Section~\ref{sec:setup}), note that $H_\tau$ is symmetric with
$H_\tau \mathbf{1} = 0$ and, by Step~2, $H_\tau \succeq p_{\min} I$
on the invariant subspace $\mathbf{1}^\perp$; hence
$H_\tau^2 \succeq p_{\min}^2 P_\perp$ and the identical witness
argument yields
$\lambda_{\max}(J_\phip^\top J_\phip) \geq
(p^{(1)}_{\min})^2 \norm{h_1}^2 / N = \Omega(M)$, matching the
statement of Lemma~\ref{lem:phi_scaling} verbatim. This completes
the proof.
\hfill$\square$

\begin{remark}[Scope, and the role of the mean-field law]
\label{rem:meanfield_scope}
The witness argument uses only: (a)~a dense classifier head over a
penultimate feature vector of dimension $\Theta(M)$;
(b)~concentration of the feature squared norm at initialization
($\norm{h}^2 \geq q_h M_h$ with probability $1 - \delta_h$)---which
variance-scaled (Kaiming-type) schemes are designed to deliver, and
which we prove nothing stronger than; and (c)~softmax cross-entropy
with $N_{cls} \geq 2$. It applies to per-pixel dense heads on MLP,
CNN (channel count $M$), and ViT (embedding dimension $M$) spatial
modules \emph{whenever hypothesis (b) holds for them}: for fully
connected modules (b) follows from standard signal propagation at
Kaiming initialization, while for CNN and ViT heads we adopt (b) as
an explicit assumption checked empirically. No i.i.d.\ Gaussian
input assumption is required---only the stated norm condition on the
features. Karakida et al.'s Theorem~4 plays the role of the
sharp asymptotic benchmark: within its model class (fully connected,
all widths proportional to $M$, squared loss), it upgrades the
witness lower bound to an exact width-linear law with explicit
constant, and their Theorem~1 supplies the complementary fact that
the \emph{mean} eigenvalue shrinks as $\mathcal{O}(1/M)$, so
curvature concentrates in a vanishing fraction of directions as the
module widens. The loss transfer from squared loss to cross-entropy
is handled exactly at initialization by Step~2, at the cost of the
constant $p_{\min}$.
\end{remark}

\begin{remark}[Empirical status]
\label{rem:phi_scaling_empirical}
Karakida et al.'s own numerical validation (their Figure~1, right
column) confirms $\lambda_{\max}$ growing linearly in $M$, matching
the exact constant of their Theorem~4, for tanh, ReLU, and linear
fully connected networks. Our Experiment~1.1 tests the law on the
SpatialMLP family, whose parameter count is \emph{linear} in width
($\Cg = \Theta(M)$, since input and output dimensions are fixed), so
the width-linear law predicts a log--log slope of $1.0$ against
$\Cg$ as well as against $M$. The measured slope is
$0.70 \pm 0.05$: the qualitative claim of
Lemma~\ref{lem:phi_scaling}---$\lambda_\phi$ grows without bound
while $\lambda_\thetap$ stays capped---is confirmed unambiguously,
but the measured exponent is sublinear, short of the width-linear
asymptote. Candidate causes (finite width, the loss-Hessian versus
Gauss--Newton measurement gap, correlated non-Gaussian features) are
discussed in Section~\ref{sec:experiments}. We report the
discrepancy rather than explain it away.
\end{remark}

\subsubsection{Proof of Lemma~\ref{lem:interlacing}}

The claim that $\lambda_{\max}(M) \geq \max\{\lambda_{\max}(A),
\lambda_{\max}(D)\}$ for a symmetric block matrix
$M = \begin{pmatrix} A & B \\ B^\top & D \end{pmatrix}$ is a direct
application of Cauchy's interlacing theorem to principal submatrices.
See Bhatia~\cite{bhatia1997matrix}, Corollary~III.1.5.

For the eigenvector $v$ achieving $\lambda_{\max}(A) = v^\top A v$,
the extension $\tilde{v} = \begin{pmatrix} v \\ 0 \end{pmatrix}$ has
$\tilde{v}^\top M \tilde{v} = v^\top A v$, so
$\lambda_{\max}(M) \geq \tilde{v}^\top M \tilde{v} / \norm{\tilde{v}}^2
= \lambda_{\max}(A)$. The same argument for $D$ completes the
proof. \hfill$\square$

\subsubsection{Proof of Lemma~\ref{lem:schur}}

The proof factors the spectral Jacobian via the chain rule and
bounds each factor's operator norm using
Assumptions~\ref{ass:linear} and~\ref{ass:inputlip}.

\paragraph{Step 1: Chain-rule factorisation.}
The residual $r$ is a composition $r(\thetap, \phip) = \tau(g_\phip(Z))$
with $Z = f_\thetap(X)$, where $\tau(\hat{y}) = \mathrm{softmax}(\hat{y})
- y_{\mathrm{onehot}}$ is the residual map from logits. The chain
rule gives
\[
    J_\thetap
    \;=\; \frac{\partial r}{\partial \thetap}
    \;=\; J_\tau \cdot J_Z \cdot \frac{\partial Z}{\partial \thetap},
\]
with $J_\tau := \partial \tau / \partial \hat{y} = \mathrm{diag}(p) -
p p^\top$ (the softmax Jacobian, which satisfies
$\|J_\tau\|_{\mathrm{op}} \leq 1$; see Botev, Ritter, and
Barber~\cite{botev2017practical}) and $J_Z := \partial g_\phip / \partial Z$.

\paragraph{Step 2: Operator-norm inequality.}
Sub-multiplicativity of the operator norm gives
\[
    \|J_\thetap\|_{\mathrm{op}}
    \;\leq\; \|J_\tau\|_{\mathrm{op}} \cdot \|J_Z\|_{\mathrm{op}}
             \cdot \left\| \frac{\partial Z}{\partial \thetap} \right\|_{\mathrm{op}}
    \;\leq\; 1 \cdot \widetilde{L} \cdot
             \left\| \frac{\partial Z}{\partial \thetap} \right\|_{\mathrm{op}},
\]
using Assumption~\ref{ass:inputlip} on the middle factor.

\paragraph{Step 3: Bounding the input-Jacobian factor.}
Under Assumption~\ref{ass:linear}, $Z_{n, h, w} = W^\top x_{n, h, w}$
with $\thetap = \mathrm{vec}(W)$, $W \in \mathbb{R}^{S \times K}$. The
per-pixel Jacobian is
$\partial Z_{n,h,w} / \partial \mathrm{vec}(W) = I_K \otimes
x_{n,h,w}^\top$
(Magnus and Neudecker~\cite{magnus1988matrix}, Ch.~9). Stacking over
the $N_{\mathrm{data}} HW$ pixel positions,
$\partial Z / \partial \thetap \in \mathbb{R}^{N_{\mathrm{data}}
HW K \times SK}$ has operator norm bounded by
\[
    \left\| \frac{\partial Z}{\partial \thetap} \right\|_{\mathrm{op}}^{2}
    \;\leq\; K \cdot \lambda_{\max}(\Sigma_X),
\]
where $\Sigma_X$ is the aggregated per-pixel input Gram (empirical
covariance) and Assumption~\ref{ass:subg} guarantees
$\lambda_{\max}(\Sigma_X) < \infty$ with high probability.

\paragraph{Step 4: Assembling.}
Combining Steps 2 and 3, and using $\lambda_{\max}(J_\thetap^\top
J_\thetap) = \|J_\thetap\|_{\mathrm{op}}^{2}$,
\[
    \lambda_{\max}(J_\thetap^\top J_\thetap)
    \;\leq\; \widetilde{L}^{2} \cdot K \cdot \lambda_{\max}(\Sigma_X)
    \;=:\; C_\thetap.
\]
The constant $C_\thetap$ depends on the classifier's input
Lipschitz constant $\widetilde{L}$ (which may grow with the depth of
$g_\phip$ but not with its width $M$; see the discussion of
Assumption~\ref{ass:inputlip}), the bottleneck dimension $K$, and
the top data-covariance eigenvalue $\lambda_{\max}(\Sigma_X)$. It
does not depend on $M$ or on the spatial parameter count $\Cg$. This
completes the proof.
\hfill$\square$

\begin{remark}[Why an operator-norm cap suffices]
An alternative route via the smallest positive eigenvalue
$\lambda_{\min}^{+}(J_\thetap^\top J_\thetap)$ requires either an
exact Kronecker factorisation (valid only for per-pixel classifiers)
or a trace-over-rank argument via the softmax residual
dimension---both of which need additional structural assumptions on
$g_\phip$. The operator-norm bound above is significantly simpler,
applies to general nonlinear spatial classifiers (CNN, ViT), and is
exactly what Theorem~\ref{thm:hessian} needs: a $\Cg$-independent
cap on the spectral block's \emph{top} eigenvalue, so that
$\mathcal{D}_{\mathrm{curv}} = \lambda_{\max}(J_\phip^\top
J_\phip)/\lambda_{\max}(J_\thetap^\top J_\thetap) \geq
\Omega(M)/C_\thetap$.
\end{remark}

\begin{remark}[Softmax Jacobian bound]
The bound $\|\mathrm{diag}(p) - p p^\top\|_{\mathrm{op}} \leq 1$ is
standard: $\mathrm{diag}(p) - p p^\top$ is the covariance matrix of
the categorical distribution with parameter $p$ (equivalently, the
Jacobian of the softmax map), hence positive semi-definite with
$\lambda_{\max} \leq \max_i p_i \leq 1$. See Botev, Ritter, and
Barber~\cite{botev2017practical} for its role in Gauss--Newton
computations.
\end{remark}

\subsubsection{Combining the lemmas: full proof of Theorem~\ref{thm:hessian}}
\label{sec:thm1_combine}

Assemble Lemmas~\ref{lem:phi_scaling} and~\ref{lem:schur} to complete
the proof. By Lemma~\ref{lem:phi_scaling}, in expectation over random
Kaiming (variance-scaled Gaussian) initialization,
\[
    \lambda_{\max}(J_\phip^{(M)\top} J_\phip^{(M)})
    \;\geq\; c_\phi \cdot M
\]
for a constant $c_\phi > 0$ independent of $M$, given explicitly by
the witness bound in the proof of Lemma~\ref{lem:phi_scaling}
($c_\phi = (1 - \delta_h - \delta_p)\, c_p q_h M_h / (N M)$); within
Karakida et al.'s~\cite{karakida2019universal} proportional-width
model class, their Theorem~4 sharpens the width-linear rate to an
exact law. By
Lemma~\ref{lem:schur},
\[
    \lambda_{\max}(J_\thetap^\top J_\thetap) \;\leq\; C_\thetap
\]
with $C_\thetap$ independent of $M$ and $\Cg$. Dividing,
\begin{equation}
    \mathcal{D}_{\mathrm{curv}}^{(M)}
    \;=\; \frac{\lambda_{\max}(J_\phip^{(M)\top} J_\phip^{(M)})}
                {\lambda_{\max}(J_\thetap^\top J_\thetap)}
    \;\geq\; \frac{c_\phi \cdot M}{C_\thetap}
    \;=\; c_1 \cdot M,
    \label{eq:disparity_bound}
\end{equation}
where $c_1 := c_\phi / C_\thetap$ depends only on the initialization
scheme, the architecture class of $g_\phip$, the classifier's input
Lipschitz constant $\widetilde{L}$, the bottleneck dimension $K$,
the input spectral dimension $S$, and the data
distribution---exactly the dependencies claimed in the theorem
statement. A subtracted constant $c_2 \geq 0$ absorbs
finite-width corrections to the mean-field limit. Under
$\Cg = \Theta(M^2)$, $M = \Theta(\sqrt{\Cg})$ and the ratio form
$\mathcal{D}_{\mathrm{curv}} \geq c_1' \sqrt{\Cg / \Cf} - c_2$
follows on multiplying and dividing by $\sqrt{\Cf}$. This completes
the proof.
\hfill$\square$

\begin{remark}[What Theorem~\ref{thm:hessian} does and does not claim]
\label{rem:thm1_scope}
Theorem~\ref{thm:hessian} is a scaling law for the curvature ratio
between two Gauss-Newton blocks; it does not claim that the full
joint Gauss-Newton matrix has a large condition number in the strict
$\lambda_{\max}/\lambda_{\min}$ sense (which is trivially infinite
under rank-deficiency), nor that joint gradient descent produces
disparate effective learning rates (which is the separate content of
Theorem~\ref{thm:twoscale}, under its own hypotheses). The scaling
$\mathcal{D}_{\mathrm{curv}} = \Omega(M)$ (equivalently
$\Omega(\sqrt{\Cg / \Cf})$ for $\Theta(M^2)$-parameter families) is a
purely geometric statement about how curvature concentrates in the
downstream module as the spatial network widens.
\end{remark}

\begin{remark}[Width scaling and the empirical exponent]
The $\sqrt{\Cg}$ scaling reflects the fact that Karakida's
underlying result~\cite{karakida2019universal} (Theorem~4) gives
$\lambda_{\max}$ in terms of the network's characteristic
\emph{width} $M$, not its total parameter count. For a fixed-depth
architecture with $\Cg = \Theta(M^2)$, the equivalent scaling is
$\lambda_{\max}(J_\phip^\top J_\phip) \sim \sqrt{\Cg}$
(log--log slope $0.5$ against $\Cg$). Experiment~1.1's SpatialMLP is
\emph{not} in this class: its parameter count is linear in width
($\Cg = \Theta(M)$), so the applicable prediction there is slope
$1.0$ against $\Cg$, and the measured $0.70 \pm 0.05$ is sublinear
(Remark~\ref{rem:phi_scaling_empirical}). The two parameter-count
forms must not be conflated; the theorem's native variable is $M$.
\end{remark}


\subsection{Supplementary material for Theorem~\ref{thm:twoscale}}
\label{sec:proof_thm2}

The main-text proof of Theorem~\ref{thm:twoscale} is complete for the
gradient-flow model~\eqref{eq:gf}: the displacement bound follows by
direct integration of the residual envelope, with the operator-norm
cap supplied by Lemma~\ref{lem:schur}. The supplementary work
concerns the hypotheses and the discrete extension.

\TODO{Remaining supplement work for Theorem~\ref{thm:twoscale}:
(i) discrete-SGD extension via Robbins--Monro tracking
(Borkar~\cite{borkar2008stochastic}, Ch.~2, Thm.~2), with gradient
noise entering the constants; (ii) informal NTK derivation of the
shortcut fitting rate $\mu(\Cg)$ for the linearized fast subsystem,
adapting Pezeshki et al.~\cite{pezeshki2021gradient}; (iii) empirical
verification that measured residual trajectories on the synthetic
problems are enveloped by $C/(1+\mu t)$, with fitted $\mu$ increasing
in $D$ (data available from Experiment~1.2 logs).}

\subsection{Empirical robustness}

We here report robustness experiments referenced in the main text.

\subsubsection{Optimizer ablation (SGD vs Adam)}
\TODO{Phase 5 follow-up. Show EGR collapse pattern in both regimes;
note Adam masks the natural gradient ratio.}

\subsubsection{Loss-variant ablation}
\TODO{Phase 5 follow-up. Cross-entropy, focal loss, label smoothing.}

\subsubsection{Regularizer ablation}
\TODO{Phase 5 follow-up. Weight decay, dropout, batch norm.
Pezeshki et al.~\cite{pezeshki2021gradient}, App.~B, already show
gradient starvation is robust to these; we reproduce briefly in our
setting.}

\subsubsection{Smallest capacity ratio where the pathology disappears}
\TODO{Phase 5 follow-up. Sweep $\Cg/\Cf$ from 1 to 1000, identify
the threshold below which joint training reaches frozen
performance.}
